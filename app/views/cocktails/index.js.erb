document.getElementById("cocktails-list").innerHTML = "<%= j render partial: "cocktails-list" %>";

document.querySelector('#ModalCocktailsFilter button').click();   // Close Modal

document.getElementById('only_this').checked = false;   // Clear the 'Only this' option

// Get the list of Filter select options
var selectOptions = document.querySelectorAll('#ingredients_ option, #categories_ option');

// Clear the Filter select options
for (let i = 0; i < selectOptions.length; i++) {
  selectOptions[i].selected = false;
}

smoothPagination();   // Make page navigation smoother

// I am looking for a way to import this functions instead of repeating the code...

// Function that makes the pagination anchors scroll up the beginning of the Cocktails list
function smoothPagination() {
  const anchors = document.getElementsByClassName("page-link"); // Get the anchors of the pagination

  // For each of the anchors do...
  for (let anchor of anchors) {
    const anchorRef = anchor.href;

    // Make sure the anchor has a page destination
    if (typeof anchorRef !== "undefined") {
      anchor.setAttribute("data-remote", "true"); // Ajaxify the anchor

      // Upon clicking on the anchor, scroll to the beginning of the Cocktails list
      smoothScrolling(anchor, '#container-cards', 250)
    }
  }
}

// jQuery Function that smooths same page scrolling
// Arguments: from: element of the starting point
//            to:   identifier of the destination element
//            time: time in milliseconds the animation takes
function smoothScrolling(from, to, time = 800) {
  from.addEventListener('click', (event) => {
    event.preventDefault(); // Prevents default anchor click behavior

    // Performs the scrolling animation during a certain period of time
    $("html, body").animate( {
      scrollTop: $(to).offset().top
    }, time);
  });
}
