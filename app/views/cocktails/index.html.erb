<div class="banner">
  <div class="container">

    <h1>Le Wagon's Bar</h1>

    <p>The best cocktails in a single place</p>

    <%= link_to "Add a Cocktail", new_cocktail_path, data: { toggle: "modal", target: "#ModalNewCocktail" }, class: "btn btn-gradient" %>

  </div>

  <div class="scroll-down">
    <%= link_to "#cards" do %>
      <i class="fas fa-angle-double-down"></i>
    <% end %>
  </div>
</div>

<div class="container-cards">
  <div id="cards">

    <% @cocktails_array.each do |cocktail| %>
      <%= link_to cocktail_path(cocktail) do %>
        <div class="card" data-aos="fade-zoom-in" data-aos-duration="400">
          <div class="img-card" style="background-image: url('<%= cl_image_path cocktail.photo.key, height: 250, width: 250, crop: :fill %>')"></div>
          <div class="text-card">
            <h5><%= cocktail.name %></h5>
            <% if cocktail.reviews.empty? %>
              <p>Not rated</p>
            <% else %>
              <p><%= cocktail.rating_average.round(1) %> ★</p>
            <% end %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

<!-- Modal Cocktail New Form -->
<div class="modal fade" id="ModalNewCocktail" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title">New Cocktail</h4>
        <button type="button" class="close" data-dismiss="modal">&times;</button>
      </div>
      <div class="modal-body">
        <%= render "cocktails/form-new" %>
      </div>
    </div>
  </div>
</div>
